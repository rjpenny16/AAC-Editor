# Runtime dependencies.
# The app edits the TD Snap page set file directly (SQLite, via the Python
# standard library), so no GUI-automation packages are required.
requests>=2.32.0

# Development / testing only:
#   pip install pytest
#   python -m pytest
